name: profile
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  shared_preferences: ^2.0.15
  flutter_svg: ^1.0.3
  lottie: ^1.3.0
  pedantic: ^1.11.1
  provider: ^6.0.2
  logger: ^1.1.0
  flutter_screenutil: ^5.5.3+1 # for responsive dimensions
  fluttertoast: ^8.0.9 # for showing toast
  flutter_bloc: ^8.0.1 # Flutter Bloc for Event & State management
  kiwi: ^4.0.2 # For Dependency Injection
  flutter_spinkit: ^5.1.0
  url_launcher: ^6.1.2
  material_design_icons_flutter: ^5.0.6595
  font_awesome_flutter: ^10.1.0
  path_provider: ^2.0.10 # for getting storage path
#  permission_handler: ^8.2.5 # for handling permission
  firebase_core: ^1.17.0
  cached_network_image: ^3.2.1
  flutter_brand_palettes: ^0.4.1
  velocity_x: ^3.4.0
  flutter_emoji: ^2.4.0
  percent_indicator: ^4.2.2
  video_player: ^2.4.2
  email_validator: ^2.0.1
  http: ^0.13.4
  nanoid: ^1.0.0
  otp_text_field: ^1.1.3
  json_annotation: ^4.5.0
  package_info_plus: ^1.4.2
  flutter_oss_licenses: ^2.0.0+1
  in_app_review: ^2.0.4
  flutter_inappwebview: ^5.4.3+7
  scan: ^1.6.0
  graphql_flutter: ^5.1.0
  wallet_connect: ^1.0.2
  eth_sig_util: ^0.0.7
  web3dart:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.1.11
  flutter_launcher_icons: ^0.9.2
  kiwi_generator: ^4.0.2
  json_serializable: ^6.2.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/png/launcher.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icon/
    - assets/lottie/
    - assets/png/
    - assets/svg/
    - assets/json/